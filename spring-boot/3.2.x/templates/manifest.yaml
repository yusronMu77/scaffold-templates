name: "Templates"
description: "Required base axis - defines what kind of artefact to generate"
required: true

values:
  - name: "services"
    description: "Spring Boot service template (function + protocol selector chain)"
    default: true
  - name: "libs"
    description: "Reusable library template (category selector)"
  - name: "parent"
    description: "Parent POM template (no further selection - replaces the old scaffold init)"
