# Intentionally empty. This is the default value of the `--mode` selector ../jig.yaml declares
# (scaffold-cli#69) - the walk descends here and finds nothing further to add, so a plain
# `--structure=mvc` create stays byte-for-byte identical to every one before this folder existed.
# See ../entity/jig.yaml for the other value.
